# ── Local Supabase (supabase start) ──────────────────────────────────────────
DATABASE_URL="postgresql://postgres:postgres@127.0.0.1:54322/postgres"
DIRECT_URL="postgresql://postgres:postgres@127.0.0.1:54322/postgres"

# Migrations: `bun run db:migrate` (uses DIRECT_URL). Not part of pre-commit —
# timing is an open decision in docs/workstreams.md (W5 migrate discussion).

# ── Remote Supabase (production / cloud) ─────────────────────────────────────
# DATABASE_URL  → port 6543, ?pgbouncer=true  → runtime (Worker, scripts) — Supavisor pooler
# DIRECT_URL    → port 5432, no pooler        → drizzle-kit migrate / push / studio
# DATABASE_URL="postgresql://postgres.[ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres?pgbouncer=true"
# DIRECT_URL="postgresql://postgres.[ref]:[password]@aws-0-[region].pooler.supabase.com:5432/postgres"
