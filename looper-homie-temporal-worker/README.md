# Looper — Homie Temporal worker on k3s

Target: `looper-homie-temporal-worker/`

Adds Facebook Temporal worker (+ Temporal) to Homie GitOps YAML for staging.

## Run

Follow `RUN_IN_SESSION.md` (preferred) or `python3 run-loop.py` after compile.

## Non-goals

Live secrets, full Zot/Kaniko pin pipeline, git worktrees.
