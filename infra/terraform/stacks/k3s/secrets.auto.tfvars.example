# Copy to secrets.auto.tfvars (gitignored). Never commit the real key.
#
# Create at https://login.tailscale.com/admin/settings/keys
# Recommended: Reusable = yes, Ephemeral = no, Tags = tag:k3s (if your ACL uses tags)

tailscale_auth_key = "tskey-auth-…"

# Leave empty when using Tailscale (default). Public 22/6443 stay closed.
admin_cidrs = []
