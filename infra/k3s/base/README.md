# Homie k3s base

Shared app workload manifests will live here later.

**Phase 1:** empty on purpose — platform packs (monitoring / Argo CD / Workflows /
ci-lane) do not require product Deployments. Lane overlays only create namespaces.
