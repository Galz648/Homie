apiVersion: v1
kind: Service
metadata:
  name: homie-ingest
spec:
  type: NodePort
  # Fixed port so DO firewall rule (tcp/31212) stays valid across applies.
  ports:
    - name: http
      port: 8080
      targetPort: http
      nodePort: 31212
