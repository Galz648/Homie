apiVersion: v1
kind: ConfigMap
metadata:
  name: fb-scrape-worker-env
  labels:
    app.kubernetes.io/name: fb-scrape-worker
    app.kubernetes.io/part-of: homie
data:
  # Temporal → CF Agent (workers.dev). Path /webhooks/:postId appended by notify.
  HOMIE_CF_AGENT_WEBHOOK_URL: https://homie-listing-extract.zafargal.workers.dev
