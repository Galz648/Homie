apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: homie-production
resources:
  - namespace.yaml
  # Migrate Supabase schema from main before production workers land.
  - ../../base/scrape-db-migrate
  # App workloads land in ../../base later — platform-only for now.
patches:
  - path: patch-scrape-db-migrate-env.yaml
