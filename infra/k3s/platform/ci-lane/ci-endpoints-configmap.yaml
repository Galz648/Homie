apiVersion: v1
kind: ConfigMap
metadata:
  name: homie-ci-endpoints
  namespace: homie-ci
  labels:
    app.kubernetes.io/part-of: homie
    homie.io/lane: ci
data:
  # Placeholder endpoints for future in-cluster CI jobs.
  # Update when services exist under homie-ci.
  CLUSTER: "local-k3s"
  NOTE: "scaffold-only"
