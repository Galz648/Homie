apiVersion: argoproj.io/v1alpha1
kind: Workflow
metadata:
  generateName: homie-ci-smoke-
  namespace: argo
  labels:
    app.kubernetes.io/part-of: homie
spec:
  workflowTemplateRef:
    name: homie-ci-smoke
